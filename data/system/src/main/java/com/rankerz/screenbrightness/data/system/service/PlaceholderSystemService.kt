package com.rankerz.screenbrightness.data.system.service

interface PlaceholderSystemService {
    // Define methods for interacting with system services (e.g., brightness, accessibility)
    fun getSystemSetting(): String
}