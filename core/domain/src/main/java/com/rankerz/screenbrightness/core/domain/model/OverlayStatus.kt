package com.rankerz.screenbrightness.core.domain.model

// Placeholder data class for OverlayStatus
data class OverlayStatus(
    val isVisible: Boolean,
    val currentBrightness: Float?,
    val currentColorTemperature: Int?
    // Add other relevant overlay status info if needed
)